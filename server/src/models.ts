export type TrackModel ={
    id: number,
    title: string,
    authorId: string;
  thumbnail: string;
  length: number;
  modulesCount: number;
}
export type AuthorModel ={
  id:string
  name:string
  photo:string
}